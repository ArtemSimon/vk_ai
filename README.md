
Поддерживаемые форматы:
- ✅ `.txt` — **полностью автоматически** (извлечение текста работает)
- 🟡 `.pdf`, `.docx` — **метаданные извлекаются**, текст — в разработке 

---

## 🔗 2. Демонстрация

| Компонент | Ссылка |
|----------|--------|
| 🤖 Telegram-бот | [`@doc_extractor_demo_bot`](https://t.me/TG_vk_ai_test_bot) |
| ⚙️ Workflow (n8n, cloudpub) | [https://ceaselessly-attentive-diamondback.cloudpub.ru/] |

---

## 🛠 3. Технологический стек

- **Telegram Bot API** — приём и маршрутизация файлов  
- **n8n (cloudpub)** — оркестрация workflow  
  - `Telegram Trigger`, `Get File`  
  - `Extract from File` (для `.txt`)  
  - `Google Drive API` (загрузка)  
- **Google Docs API** — в интеграции (конвертация `.docx` → в доработке)  

Все credentials хранятся в защищённом хранилище n8n (OAuth2), без открытых токенов.

---

## 📁 4. Структура workflow

1. **Telegram Trigger**  
   - Принимает сообщения с документами  
   - Извлекает `file_id`, `file_name`, `mime_type`, `file_size`

2. **Telegram → Get File**  
   - Скачивает бинарные данные (`binary.data`)

3. **Switch по расширению**  
   - `.txt` → `Extract from Text File` → `Google Sheets`  
   - `.pdf`, `.docx` → `Upload to Google Drive` → `Google Sheets` (метаданные)
---

## ⚠️ 5. Ограничения и пути развития

| Проблема | Причина | Решение (в разработке) |
|---------|---------|------------------------|
| Нет автоматического извлечения текста из `.docx` |
| Безопасность передачи файлов | Файлы временно хранятся в Google Drive | ➤ Автоматическое удаление после обработки |
| Поддержка сканов (PDF с изображениями) | Google Docs API не делает OCR по умолчанию | ➤ Интеграция Google Cloud Vision API (план на этап 2) |

---

## 📝 6. Инструкция по запуску 

> Для полной автоматизации (включая `.docx`) n8n.

1. **Подготовка**
   ```bash
   git clone https://github.com/ArtemSimon/vk_ai.git
   cd vk_ai 
   cd n8n-compose